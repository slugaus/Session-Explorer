# Session-Explorer
A portable lightweight application for analysing session event logs.

# To do
- ingest windows event log data (4624, 4647/4634, 4625)
- ingest pcap data 
- timeplot page for windows eventlogs (x = time, y = user)
  - logon session history per user
  - failed logon attempts per user
  - allow multiple hosts to be shown concurrently 
- timeplot page for pcap data (x = time, y = protocol)
  - tcp/udp session history per protocol
  - automatic detection of suspicious patterns
    
